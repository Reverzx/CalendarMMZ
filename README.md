# Calendar Application

Полнофункциональное приложение календаря с веб-интерфейсом и интеграцией с Telegram ботом.

## Возможности

- 📅 Веб-интерфейс календаря с визуальным отображением событий
- 🤖 Telegram бот для управления событиями
- 💾 PostgreSQL база данных для хранения событий
- 🐳 Docker контейнеризация для простого развертывания
- ✨ Создание, редактирование и удаление событий
- 📱 Просмотр событий на сегодня, завтра или на неделю

## Технологии

- **Backend**: Python 3.11, Flask, SQLAlchemy
- **Frontend**: HTML, CSS, JavaScript
- **Database**: PostgreSQL
- **Bot**: python-telegram-bot
- **Deployment**: Docker, Docker Compose

## Структура проекта

```
CalendarClaude/
├── app/
│   ├── __init__.py          # Инициализация Flask приложения
│   ├── models.py            # Модели базы данных
│   ├── routes.py            # API endpoints
│   ├── telegram_bot.py      # Telegram бот
│   └── templates/
│       └── calendar.html    # Веб-интерфейс календаря
├── static/
│   ├── css/
│   │   └── style.css        # Стили
│   └── js/
│       └── calendar.js      # Клиентская логика
├── requirements.txt         # Python зависимости
├── config.py                # Конфигурация приложения
├── run.py                   # Запуск веб-приложения
├── bot.py                   # Запуск Telegram бота
├── Dockerfile               # Docker образ для веб-приложения
├── Dockerfile.bot           # Docker образ для бота
└── docker-compose.yml       # Оркестрация контейнеров
```

## Установка и запуск

### Предварительные требования

- Docker и Docker Compose установлены на вашей системе
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd CalendarClaude
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваши данные:

```env
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://calendar_user:calendar_pass@db:5432/calendar_db
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
WEBHOOK_URL=http://localhost:5000
```

### Шаг 3: Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен и вставьте его в `.env` файл

### Шаг 4: Запуск приложения с Docker

```bash
docker-compose up -d
```

Эта команда запустит три контейнера:
- `calendar_db` - PostgreSQL база данных
- `calendar_web` - Flask веб-приложение
- `calendar_bot` - Telegram бот

### Шаг 5: Проверка работы

Веб-интерфейс будет доступен по адресу: [http://localhost:5000](http://localhost:5000)

Откройте Telegram и найдите вашего бота по имени, которое вы указали при создании.

## Использование

### Веб-интерфейс

1. Откройте браузер и перейдите на [http://localhost:5000](http://localhost:5000)
2. Используйте календарь для просмотра событий
3. Нажмите на день для создания нового события
4. Используйте кнопки "Редактировать" и "Удалить" для управления событиями

### Telegram бот

Доступные команды:

- `/start` - Начать работу с ботом
- `/addevent` - Создать новое событие (интерактивный процесс)
- `/myevents` - Показать все ваши события
- `/today` - Показать события на сегодня
- `/tomorrow` - Показать события на завтра
- `/week` - Показать события на ближайшую неделю
- `/help` - Показать справку

### Создание события через бота

1. Отправьте `/addevent`
2. Введите название события
3. Введите описание (или напишите "пропустить")
4. Введите дату и время начала (формат: ДД.ММ.ГГГГ ЧЧ:ММ)
5. Введите дату и время окончания (формат: ДД.ММ.ГГГГ ЧЧ:ММ)

## API Endpoints

### GET /api/events
Получить список всех событий

**Query параметры:**
- `start` - Фильтр по дате начала (ISO format)
- `end` - Фильтр по дате окончания (ISO format)

### POST /api/events
Создать новое событие

**Body:**
```json
{
  "title": "Название события",
  "description": "Описание",
  "start_time": "2024-12-25T15:00:00",
  "end_time": "2024-12-25T16:00:00"
}
```

### PUT /api/events/{id}
Обновить существующее событие

### DELETE /api/events/{id}
Удалить событие

## Управление Docker контейнерами

### Просмотр логов

```bash
# Все контейнеры
docker-compose logs -f

# Только веб-приложение
docker-compose logs -f web

# Только бот
docker-compose logs -f bot

# Только база данных
docker-compose logs -f db
```

### Остановка приложения

```bash
docker-compose down
```

### Остановка с удалением данных

```bash
docker-compose down -v
```

### Перезапуск контейнеров

```bash
docker-compose restart
```

### Пересборка образов

```bash
docker-compose up -d --build
```

## Разработка без Docker

### Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

pip install -r requirements.txt
```

### Запуск PostgreSQL локально

Установите PostgreSQL и создайте базу данных:

```sql
CREATE DATABASE calendar_db;
CREATE USER calendar_user WITH PASSWORD 'calendar_pass';
GRANT ALL PRIVILEGES ON DATABASE calendar_db TO calendar_user;
```

### Запуск веб-приложения

```bash
python run.py
```

### Запуск Telegram бота

В отдельном терминале:

```bash
python bot.py
```

## Troubleshooting

### Проблема: Бот не отвечает

1. Проверьте, что TELEGRAM_BOT_TOKEN правильно указан в .env
2. Проверьте логи бота: `docker-compose logs -f bot`
3. Убедитесь, что контейнер бота запущен: `docker-compose ps`

### Проблема: Не подключается к базе данных

1. Проверьте, что контейнер базы данных запущен: `docker-compose ps`
2. Проверьте логи базы данных: `docker-compose logs -f db`
3. Убедитесь, что DATABASE_URL правильно указан в .env

### Проблема: Веб-интерфейс не открывается

1. Проверьте, что контейнер веб-приложения запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs -f web`
3. Убедитесь, что порт 5000 не занят другим приложением

## Безопасность

- Измените `SECRET_KEY` в `.env` на случайную строку для production
- Используйте сильные пароли для базы данных
- Не коммитьте `.env` файл в git
- Настройте firewall для ограничения доступа к базе данных
